FROM rust as builder

RUN apt-get update \
 && apt-get install -y libssl-dev --no-install-recommends

COPY ./ /usr/src/sccache/
WORKDIR /usr/src/sccache
RUN cargo build --release --features="dist-client dist-server"

